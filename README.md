# TesteAPIVersionamento
Testando o versionamento de APIs com ASP.NET Core

## Para rodar o projeto
Use o comando `dotnet run`

Acesse a url `http://localhost:5000/swagger`

Todos os dados ficam armazenados **somente em memória** e **não** são persistidos ao fim da execução da aplicação.
